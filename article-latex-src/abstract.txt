Example abstract.

The abstract is in a separate plain text file, to make it easier to extract and
paste into other places.

This file can include TeX special characters like %, &, #, ^, _, ~, or $ with no escapes.

If you don't need an abstract, just delete the file.
